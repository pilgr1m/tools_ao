import { useHttp } from '../services'
import { bagsURL, capesURL } from '../../consts'

const { request } = useHttp()

export const a = 1

// export const accessoriesService = {
//   bags: () => request(bagsURL),
//   capes: request(capesURL),
// }
